# Using spring-ai-starter-mcp-server-webmvc
spring:
  ai:
    mcp:
      server:
        name: stdio-mcp-server-demo
        version: 1.0.0
        type: SYNC

  #main:
    #banner-mode: off
    #web-application-type: none

#logging:
  #pattern:
    #console: